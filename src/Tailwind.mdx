import { Meta } from '@storybook/addon-docs/blocks';

export const rgbToHex = (rgb) => {
  const [r, g, b] = rgb.split(' ');
  return '#' + [r, g, b].map(x => parseInt(x).toString(16).padStart(2, '0')).join('');
};

export function twValue(key) {
  const raw = getComputedStyle(document.documentElement)
    .getPropertyValue(key)
    .trim();

  // Convert "48rem" or "768px" → px (number)
  if (raw.endsWith('rem')) {
    const root = parseFloat(getComputedStyle(document.documentElement).fontSize); // default 16
    return `${parseFloat(raw) * root}px`;
  }
  return `${parseFloat(raw)}px`;
}

<Meta title="Tailwind Configuration" />

# Tailwind Configuration

To add Kubetail-UI's custom Tailwind theme to your project with Tailwind v4, add the Tailwind plugin to your Vite config:

```typescript
import tailwindcss from '@tailwindcss/vite';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [
    tailwindcss(),
    // other plugins...
  ],
});
```

Then import Tailwind CSS and the kubetail-ui styles in your main CSS file:

```css
@import "tailwindcss";
@import "@kubetail/ui";
```

## Breakpoints

<table>
  <tbody>
    <tr>
      <td>sm</td>
      <td>{twValue('--breakpoint-sm')}</td>
    </tr>
    <tr>
      <td>md</td>
      <td>{twValue('--breakpoint-md')}</td>
    </tr>
    <tr>
      <td>lg</td>
      <td>{twValue('--breakpoint-lg')}</td>
    </tr>
    <tr>
      <td>xl</td>
      <td>{twValue('--breakpoint-xl')}</td>
    </tr>
    <tr>
      <td>2xl</td>
      <td>{twValue('--breakpoint-2xl')}</td>
    </tr>
  </tbody>
</table>
