import React from 'react';
type Props = {
    as?: React.ElementType;
    children?: React.ReactNode;
    className?: string;
};
declare const Container: ({ as, children, className }: Props) => React.JSX.Element;
export default Container;
