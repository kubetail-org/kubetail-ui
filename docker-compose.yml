services:
  app:
    build:
      dockerfile_inline: |
        FROM node:22.16.0-alpine3.22
        WORKDIR /app
        RUN corepack enable
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - 6006:6006
    command: sh -c "pnpm install && pnpm run storybook --no-open"

